[default.extend-words]
# "lancache" and its ecosystem spell things their own way.
lancache = "lancache"

[files]
extend-exclude = [
    "LICENSE",
    ".agent/grafana/navigator-dashboard.json",
    # Fuzz corpora are deliberately malformed input, not prose.
    "fuzz/corpus/",
    "fuzz/artifacts/",
]
